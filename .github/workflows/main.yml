name: Tests

on:
  push:
    tags:
      - '!refs/tags/*'
    branches:
      - '*'

jobs:
  run_tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]

    steps:
      - uses: actions/checkout@master

      - name: Install dependencies and run tests
        run: |
          pip install --no-cache-dir --quiet -U pip
          pip install --no-cache-dir --quiet wheel
          pip install --no-cache-dir --quiet ansible docker
          pip install --no-cache-dir --quiet --user molecule
          molecule test
